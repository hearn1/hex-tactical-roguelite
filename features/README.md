# Feature Specs

Each `features_NN_*.md` file in this directory is a single, session-sized implementation slice. The implementation order, dependencies, and session-prompt format are in [PLAN.md](../PLAN.md) at the repo root.

GitHub issues mirror these files 1:1 — open an issue to track work in progress, but the file is the canonical spec.
